#include "gtest/gtest.h"

TEST(Security, Signing)
{
  ASSERT_TRUE(true);
}

TEST(TCP, localFile)
{
  ASSERT_TRUE(true);
}

TEST(TCP, remoteFile)
{
  ASSERT_TRUE(true);
}

int main(int argc, char **argv) {
  ::testing::InitGoogleTest(&argc, argv);
  return RUN_ALL_TESTS();
}